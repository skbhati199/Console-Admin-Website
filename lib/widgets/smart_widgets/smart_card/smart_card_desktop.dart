part of smart_card_widget;
class _SmartCardDesktop extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('smart_card_desktop'),
    );
  }
}